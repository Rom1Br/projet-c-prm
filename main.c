#include <stdio.h>

#include #load_data.h"
#include "start.h"
#include "play_game.h"

int main() {
    // Load data from external Web API if not found
    // Ask for username
    // Ask for cash amount at the beginning
    // While November is not over, each day:
    //   Ask user what command he wants to input (e.g. SELL AAPL 13 / NEXT)
    //   Show results and ask for confirmation
    //   Save results in a file
    // Show final results and save results in file
    return 0;
}